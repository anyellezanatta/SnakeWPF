<UserControl x:Class="Snake.Control.UserPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="600" Background="{x:Null}">
    
    <Grid Background="#FF7C7C7C">
        <TextBlock Text="{Binding pontuacao, StringFormat='Pontuação: {0} pontos'}" FontFamily="Verdana" FontSize="20"
                 Margin="10,2,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="White" Background="{x:Null}"/>
    </Grid>
    
</UserControl>
